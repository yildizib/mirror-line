# Architecture verification

## Requirements

- Tests MUST prevent UI-to-infrastructure direct dependencies.
- Tests MUST cover fake gateway/controller behavior for settings, permissions and installed apps.
- Existing test suite and debug APK build MUST pass before the change is considered complete.
